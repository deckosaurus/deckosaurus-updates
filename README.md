# Deckosaurus updates

The Sparkle appcast and update archives for Deckosaurus, served at https://updates.deckosaurus.com/.

Written only by `make publish` and `make promote` in the app repo. Never edit by hand.
